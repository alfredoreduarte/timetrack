# Vite Environment Variables for TimeTrack UI

# API Configuration
# For development - points to local API server
VITE_API_URL="http://localhost:3011/api"

# For production - update with your actual domain
# VITE_API_URL="https://api.yourdomain.com/api"

# Development Configuration
# Vite dev server port (default: 5173)
# This is handled by vite.config.ts, not environment variables

# Build Configuration
# These are handled automatically by Vite and the build process