{
  "name": "@timetrack/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for TimeTrack",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  },
  "peerDependencies": {
    "typescript": "^5.3.2"
  }
}
